## bom-check: check (recursively) for files in a folder with utf-8 bom

usage:
```
bom-check <filters> <directory>
```
example (check for utf-8 and utf-8 with bom in current directory):
```
bom-check --utf8 --utf8-bom .
```